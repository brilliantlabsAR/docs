{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch site",
            "type": "shell",
            "command": "bundle exec jekyll serve --livereload --open-url",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}